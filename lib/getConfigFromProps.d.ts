/**
 * @description get and consolidate config from props,
 * the config of the same props should be the same
 * @param {RdpProps} props
 * @returns {(RdpConfig | undefined)}
 */
export declare const getConfigFromProps: any;
