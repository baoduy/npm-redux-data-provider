/**
 * get Store provider for cconfig,
 * the provider should be the same for a config. so this func will be memoize
 * @param config
 * @param props
 */
export declare const createStoreProviderConfig: any;
